import React from 'react';
import Chat from '../components/Chat';

const Home = () => {
    return (
        <div className="h-full flex flex-col">
            <Chat />
        </div>
    );
};

export default Home;
